%li
	%b= product.title
	= "@ $#{product.price}"
	.links
		= link_to "Details", view_path(id: product)
		\| 
		- if product.quantity < 1 
			Out of stock
		- else
			= link_to "Add to cart", to_cart_path(id: product), :method=> :post